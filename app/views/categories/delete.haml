- form_tag url(:categories, :destroy, :id => @category.id), :method => "delete" do

  %p Are you sure you want to delete this category?

  %ul
    %li== <b>#{@category.name})</b>
  
  %p
    = submit_tag "Yes, delete"
    or
    = link_to "No, never mind", url(:categories, :index)