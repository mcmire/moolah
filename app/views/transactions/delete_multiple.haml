- form_tag url(:transactions, :destroy_multiple), :method => "delete" do

  - for id in @ids
    %input(type="hidden" name="to_delete[]" value="#{id}")

  %p Are you sure you want to delete these transactions?

  %ul
    - for transaction in @transactions
      %li== <b>#{transaction.original_description}: #{transaction.amount_as_currency} (#{transaction.settled_on})</b>
  
  %p
    = submit_tag "Yes, delete"
    or
    = link_to "No, never mind", url(:transactions, :index)