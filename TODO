TODO

X Javascript confirmation on multiple and single delete, with fallback (a little jQuery plugin)
X Tabs for checking and savings account
X Ability to edit description, category, and other details of a transaction
X Ability to add a transaction manually
X Destroy multiple is broken?
* Graphs (using jqplot)
  - overall balance
  - monthly and biweekly income
  - monthly and biweekly expenses (by category)
  - monthly and biweekly net worth
  - monthly and biweekly income rate (with avg line)
* Add categories w/ category management
* Add transaction types w/ transaction type management
* Auto-assignment of category, transaction type, and description upon import
* Switch to Mongoid
* Enforce Ruby 1.9?
* Store amount in cents, silly!
* Make integration stuff into a gem and require that instead (and convert over to spec/integration)
* Paginate the list
* CSSify this biatch (reset stylesheet, vertical rhythm, etc.) and add nav links
* Javascript add/edit (click on a line to edit, click out to cancel)
* Upon import, show a line in between current import and existing data (so we know what we've imported)
* Sort by column in transactions list